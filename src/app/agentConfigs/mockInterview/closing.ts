import { AgentConfig } from "@/app/types";

export const closingAgent: AgentConfig = {
  name: "closing",
  publicDescription: "模擬面接のクロージングを担当するエージェント。",
  instructions:
    `[LLMへの内部指示: 前のエージェントから転送されました。**まず、直前のユーザーの発言（退職理由）を踏まえ、「〇〇という理由で退職（を考えている）のですね。承知いたしました。」のように応答**してください。その後、以下の言葉を続けてください。]

これで私からの質問は以上です。最後に、何か私（たち）に質問はありますでしょうか？（ユーザーの質問があれば回答し、なければ面接を終了してください）... それでは、以上で本日の模擬面接を終了とさせていただきます。お疲れ様でした。本日は貴重なお時間をいただき、誠にありがとうございました。`,
  tools: [],
  // このエージェントは最終段階なので downstreamAgents は不要
};